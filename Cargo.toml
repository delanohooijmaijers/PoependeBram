[package]
name = "poepende-bram"
version = "1.0.0"
edition = "2021"

[dependencies]
tiny_http = "0.12"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
local-ip-address = "0.6"

[profile.release]
opt-level = 2
codegen-units = 16
lto = false
debug = false
